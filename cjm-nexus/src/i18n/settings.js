/**
 * Ajustes de internacionalización — CJM Nexus.
 * Cada idioma es una URL real e indexable: /es, /en, /de (español por defecto).
 */
export const languages = ['es', 'en', 'de'];
export const defaultLanguage = 'es';

/** Locale Open Graph por idioma */
export const localeMap = { es: 'es_ES', en: 'en_US', de: 'de_DE' };
